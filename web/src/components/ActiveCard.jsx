import React from 'react'
import {Link} from "react-router-dom";

export const ActiveCard = (props) => {
    return (
        <div>
            ActiveCard
            <Link to="/">X</Link>
        </div>
    )
}
